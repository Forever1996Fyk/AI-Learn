spring:
  ai:
    mcp:
      server:
        name: spring-ai-mcp-weather
  main:
    # 禁用 SpringBoot启动时的横幅(Banner)显示，对于使用 STDOIO 传输的 MCP 服务器，禁用 Banner 有助于避免输出干扰
    banner-mode: off
server:
  port: 8086

#logging:
#  pattern:
#    console:
#  file:
#    name: /Users/kaimichael/Desktop/develop/Idea Project/personal/AI-Learn/spring-ai/log/mcpserver/mcp-weather-stdio-server.log

# 访问 OpenWeather API的密钥
tools:
  open-weather-api-key: 8154255046098978efc2b96798dd207a