spring:
  thymeleaf:
    cache: false
  ai:
    mcp:
      client:
        enabled: true
        name: mcp-client
        version: 1.0.0
        request-timeout: 60s
        type: SYNC
        sse:
          connections:
            weather-server:
              url: http://localhost:8081
              sse-endpoint: /sse