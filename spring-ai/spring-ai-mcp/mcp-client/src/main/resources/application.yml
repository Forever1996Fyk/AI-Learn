spring:
  application:
    name: McpClient
  profiles:
    active: sse
  main:
    web-application-type: none
  ai:
    dashscope:
      # 替换为自己的 api_key
      api-key: xxxxx
      chat:
        options:
          model: qwen3-max
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
server:
  port: 8061